import { ColumnDefinition } from './column-definition';

/** Holds the column definition of a select box */
export const selectRowColumnDefinition: ColumnDefinition = {
  name: 'selectRowColumn',
  type: 'SelectRowColumn',
  position: -999,
  sticky: 'start',
};
