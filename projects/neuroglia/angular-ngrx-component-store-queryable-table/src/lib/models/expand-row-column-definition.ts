import { ColumnDefinition } from './column-definition';

/** Holds the column definition of the expanded row */
export const expandRowColumnDefinition: ColumnDefinition = {
  name: 'expandRowColumn',
  type: 'ExpandRowColumn',
  position: -1000,
  sticky: 'start',
};
