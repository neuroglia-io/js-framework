import { InjectionToken } from '@angular/core';

/** An injection token used to defined if the application is running inside a shell */
export const IS_RUNNING_IN_SHELL_TOKEN = new InjectionToken<boolean>(
  'is-running-in-shell-token-3c3730137c944e29bca8c803ef5233a9',
);
