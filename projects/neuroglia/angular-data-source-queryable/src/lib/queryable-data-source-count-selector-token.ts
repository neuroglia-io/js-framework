import { InjectionToken } from '@angular/core';

export const QUERYABLE_DATA_SOURCE_COUNT_SELECTOR = new InjectionToken<<T = any>(response: any) => number>(
  'queryable-data-source-count-selector',
);
